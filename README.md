# JsonToCsv

JsonToCsv is a high-performance, stateless .NET 10 command-line tool that parses JSON datasets and flattens them into structured CSV outputs. It allows custom delimiter configurations, support for special whitespace characters, and robust verification.

## Features
- **High-Performance JSON Flattening**: Recursively traverses deep hierarchical JSON documents and flattens them to standard dot-notated tabular maps.
- **Safe Deep-Recursion Protection**: Monitors recursion stack bounds explicitly, safely processing up to 100 levels of object nesting and throwing clean errors instead of experiencing a StackOverflowException.
- **Stateless CSV Writer**: Implements dynamic stream-based CSV writing with strict O(1) memory overhead, directly serializing flattened records to a text writer stream.
- **RFC 4180-Compliant Escaping**: Handles complex cell formatting gracefully (including embedded custom delimiters, line breaks, and internal double quotes).
- **Non-Uniform Record Support**: Aligns jagged datasets sequentially with omitted key properties rendering correctly as empty table columns.
- **Custom Delimiter Selection**: Supports custom column delimiters such as commas, semicolons, and escape sequences (e.g., tab character `\t`).
- **Comprehensive CLI Parsing**: Leverages a structured, declarative argument framework via `CommandLineParser`.
- **Pipeline Friendly**: Returns standard exit codes and writes descriptive error diagnostics directly to standard error (`stderr`).

## Tech Stack
- Runtime: .NET 10 SDK (`net10.0`)
- Core Framework: `System.Text.Json` (v10.0.0 framework assembly)
- CLI Parser: `CommandLineParser` v2.9.1
- Unit Testing: `xunit` v2.9.2, `Microsoft.NET.Test.Sdk` v17.12.0

## Getting Started

### Prerequisites
- .NET 10 SDK (v10.0.100 or higher)

### Installation & Build
```sh
for i in 1 2 3; do dotnet restore --disable-parallel && exit 0; sleep 5; done; exit 1
dotnet build
```

## Usage
```sh
dotnet run --project src/JsonToCsv/JsonToCsv.csproj run -i path/to/input.json -o path/to/output.csv -d ","
```

Or using tab character as a delimiter:
```sh
dotnet run --project src/JsonToCsv/JsonToCsv.csproj run -i path/to/input.json -o path/to/output.csv -d "\t"
```

## Running Tests
```sh
dotnet test
```

## License
Apache 2.0 © 2026 The Authors

## Deployment
<!-- DEPLOYMENT_URL_START -->
<!-- DEPLOYMENT_URL_END -->

<!-- RELEASE_URL_START -->
<!-- RELEASE_URL_END -->

## Documentation
- [Changelog](CHANGELOG.md) — release history.
- [Architecture](docs/architecture_state.md) — the living architecture document.

## Generated By
This project was automatically generated by [Token Burners Factory](https://github.com/Cyberthon-2026-Token-Burners/token-burners-factory#token-burners-factory).

**Original request:** Write a .Net CLI utility that takes a JSON file as input and converts it to CSV. You should also add the ability to select a delimiter.